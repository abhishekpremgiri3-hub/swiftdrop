# SwiftDrop V10 Android build

The Android project is generated by Capacitor during the build workflow. Do not create a hand-written Android folder in GitHub.

CI sequence:
1. Install Node dependencies.
2. Run the web build.
3. Run `npx cap add android`.
4. Run `npx cap sync android`.
5. Run Gradle to create the debug APK.
